<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <!-- 
      Uncomment the following line (and set the proper path) if you need to reference 
      a Flutnet.Interop NuGet package that is yet to be published to NuGet Gallery 
      (like the temporary packages built via pack.sh script)
    -->
    <!--<add key="local-packages" value="../artifacts/nuget-packages" />-->
  </packageSources>
</configuration>